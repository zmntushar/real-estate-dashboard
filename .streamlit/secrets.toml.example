# Copy this file to secrets.toml in the same folder and fill in your key.
#
#     .streamlit/secrets.toml
#
# secrets.toml is gitignored, so your key stays out of the repository.
# This is the only key the app uses; every other panel works without one.
# Get one free and instantly at https://api.data.gov/signup/

FBI_API_KEY = "paste-your-key-here"
