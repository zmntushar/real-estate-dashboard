# Local desktop defaults. headless is left unset so Streamlit opens the
# browser automatically when start_app.bat runs it.
[browser]
gatherUsageStats = false

[server]
port = 8501

[theme]
base = "light"
primaryColor = "#0e9aa7"
